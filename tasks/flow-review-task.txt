你是专门负责 autopilot 开发流程质量的 Codex。你的任务是全面审查当前 autopilot 系统的所有脚本和流程，找出 bug、遗漏、和优化点。

## 需要审查的文件
1. ~/.autopilot/scripts/watchdog.sh — 核心守护进程（v4）
2. ~/.autopilot/scripts/tmux-send.sh — 消息发送（v3，paste-buffer）
3. ~/.autopilot/scripts/monitor-all.sh — cron 监控报告
4. ~/.autopilot/scripts/consume-review-trigger.sh — review 触发消费
5. ~/.autopilot/scripts/codex-status.sh — 状态检测
6. ~/.autopilot/scripts/auto-nudge.sh — 自动推进
7. 各项目的 status.json — 生命周期状态
8. 各项目的 prd-todo.md — 任务清单
9. 各项目的 CONVENTIONS.md — 编码规范

## 审查维度
1. **消息传递可靠性**: tmux-send.sh v3 的 paste-buffer 方案是否在所有场景下可靠？watchdog 的所有 nudge 路径是否都走了 tmux-send.sh？
2. **状态检测准确性**: detect_state() 的 7 种状态判断是否有误判场景？idle vs working 的边界条件？
3. **自动推进效果**: nudge 消息是否有效？Codex 是否真的在执行收到的指令？有没有指令被忽略的情况？
4. **review 流程闭环**: Layer 1 自动检查 → Layer 2 增量 review → 修复 → 再 review 的流程是否完整？
5. **status.json 维护**: 谁负责更新？什么时机更新？有没有自动化的可能？
6. **项目配置同步**: watchdog 和 monitor-all 的 PROJECTS 数组是否一致？
7. **错误恢复**: 脚本崩溃后能否自动恢复？launchd 重启是否可靠？
8. **日志和可观测性**: 日志是否足够排查问题？有没有关键事件缺少日志？
9. **资源管理**: 内存、磁盘、进程数是否有泄漏风险？

## 输出要求
写到 /Users/wes/clawd/AUTOPILOT-FLOW-FULL-REVIEW.md，格式：
- 按维度分组
- 每个发现：P0-P3 级别 + 描述 + 修复建议
- 末尾：总结 + 建议优先级排序
